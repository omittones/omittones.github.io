/**
 * Chat list autoscroll: jump to bottom after a user message, and follow streaming while the
 * viewport stays pinned to the bottom. Off by default until validated on target devices.
 */
export var FEATURE_CHAT_AUTOSCROLL = false;
